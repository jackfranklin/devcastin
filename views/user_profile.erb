User profile<br>
<%= @user.credits_remaining %> <%= @user.credits_remaining == 1 ? "credit" : "credits" %> remaining

<% @videos.each do |video| %>
  <%= video.title %>
<% end %>

<h2>Add coupon code</h2>
<form action="/user/add_coupon" method="post">
  <p>Code: <input type="text" name="coupon_code" /></p>
  <input type="submit" value="Go" />
</form>
