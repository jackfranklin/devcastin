development:
  clients:
    default:
      database: devcastin
      hosts:
        - localhost:27017

production:
  clients:
    default:
      uri: <%= ENV['MONGOHQ_URL'] %>

test:
  clients:
    default:
      database: devcastin_tests
      hosts:
        - localhost:27017
