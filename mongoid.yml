development:
  sessions:
    default:
      database: devcastin
      hosts:
        - localhost:27017

production:
  sessions:
    default:
      uri: <%= ENV['MONGOHQ_URL'] %>

test:
  sessions:
    default:
      database: devcastin_tests
      hosts:
        - localhost:27017
